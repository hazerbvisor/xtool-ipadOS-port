# iOS App Bootstrap

`XToolMobileApp` is the first executable target for the iOS/iPadOS port. It depends only on `XToolMobileCore` and is intended to be cross-compiled from the Linux/Android bootstrap environment before on-device compiler integration.

Current milestone: prove an arm64 iOS executable can be produced from the fork using the installed Darwin Swift SDK.
